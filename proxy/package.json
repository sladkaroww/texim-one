{
  "name": "texim-event-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "CORS-enabled TruckersMP event proxy for the TEXIM ONE website autofill",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
